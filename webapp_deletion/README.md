# Deletes listed webapps

  - ⚡ Use with caution! ⚡

  - Requires /config.yml setup by the example
  - Requires webapps domains written one per line in /webapps.txt
  - Run: `./webapp_deletion.rb` to delete webapps from the webapps.txt file
